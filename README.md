# 🤖 CriptoBot

**CriptoBot** — криптовалютный бот, реализованный на **Java**.

## 🧠 Описание

CriptoBot — это бот, написанный на Java, предназначенный для взаимодействия с криптовалютными данными.

Проект создаёт основу для:
- интеграции с внешними API криптобирж,
- обработки и анализа финансовых данных,
- использования многопоточности и современных библиотек.

Проект развивается как самостоятельный pet-project, демонстрирующий владение Java, структурой Gradle, зависимостями и архитектурой приложения.

## 🧱 Структура проекта

- `src/main/java` — исходный код проекта
- `build.gradle` — конфигурация сборки и зависимостей
- `gradlew` / `gradlew.bat` — скрипты Gradle Wrapper для кросс-платформенной сборки

## 🧰 Технологии

- **HTTP клиент (OkHttp / Retrofit)**  
- **JSON парсер (Gson / Jackson)**  
- **WebSocket для реального времени**  
- **Базы данных (SQLite / PostgreSQL / Redis)**  
- **Интеграция с API бирж (Binance, CoinGecko, Kraken и т.д.)**

## 🚀 Сборка и запуск

Чтобы собрать и запустить проект локально:

1. Клонировать репозиторий:

```bash
git clone https://github.com/Dilunder/CriptoBot.git
```

2. Перейти в папку проекта:
 
```bash
cd CriptoBot
```

3. Собрать проект с помощью Gradle Wrapper:

В Linux / macOS:

```bash
./gradlew build
```

В Windows:

```bash
gradlew.bat build
```

4. Запустить приложение:

```bash
./gradlew run
```
